/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'images.unsplash.com',
            'randomuser.me',
            'cdn-icons-png.flaticon.com'
        ],
    },
};

export default nextConfig;